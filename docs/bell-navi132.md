# ATベル押し順ナビ v132

通常のAT消化中、ベル成立BETで黄色の1・2・3をリール上部に表示。6通りの押し順を等確率で選び、各番号を対応リールに配置する。停止済みのリールからナビを消し、第三停止で全消去。

2026-09-19: 宗介の左右中ボイスを追加。BETでナビ1番のリール、第一停止完了で次のリール、第二停止完了で最後のリールを1回ずつ読み上げ、第三停止完了で `よっしゃあ_宗介.wav` を1回再生する。ユーザー提供の `左_宗介.wav`・`中_宗介.wav`・`右_宗介.wav`・`よっしゃあ_宗介.wav` を無加工で使用し、ボイス音量に従う。AUTOと途中引き継ぎも同じナビ順を使用する。停止連打で同じ段階の声は重複させず、番号のないレア役ナビ・配置プレビュー・高速試算では再生しない。押し順を間違えた場合も、画面上で次に指示している未停止リールを案内する。既存のベルナビBET効果音も維持。

2026-09-20: 空の `左_空撮り直し.wav`・`中_空.wav`・`右_空.wav` を追加。番号ナビ発生ごとに宗介・空を50%ずつ選び、`bellNaviVoiceCharacter` に記録してBETから第二停止まで同じキャラを使用。AUTO・途中引き継ぎ・押し順ミスでも再抽選しない。第三停止の「よっしゃあ」は既存の宗介ボイス。

AUTO開始済み・BET後にAUTOをON・手動で一部停止後にAUTOをON、いずれも同じナビ順を使う。狙え・特化・初期pt準備・引き戻し・裏チャレンジ・BIG中には通常AT用のベルナビを重ねない。ベルの成立抽選と払い出しは従来通りで、手動押し順ミスの減額は追加しない。遊技履歴のdetail.bellNaviOrderに指定順を記録し、実停止順と比較可能。

画面左下「ナビ調整」で調整用プレビューを表示。リール幅を基準にX・Y・幅・高さ・間隔を調整し、ブラウザーへ自動保存。「今を標準」でRESET時の戻り先も保存。リール位置・筐体サイズを変更したときも追従する。

生成済みの黄色PNGをCSSスプライトとして使用し、元画像・キャラランプの位置は変更しない。

検証：関連9テスト成功。無音の独立ブラウザーで、全6通りの手動停止とAUTO停止、途中AUTO引き継ぎ、各ベル15pt払い出し、非ベル時の非表示、プレビューの配置変更・保存・再読み込み・RESETを確認。ページエラーと素材欠落なし。記録はresearch/bell132/qa.json。
