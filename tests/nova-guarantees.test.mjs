import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';const ctx=vm.createContext({});for(const f of ['nova-art.js','nova-balance.js'])vm.runInContext(fs.readFileSync(f,'utf8'),ctx);const a=ctx.NovaArt;
test('Ouma BET settlement preserves initial plus wins and is idempotent',()=>{let s=a.startZone(a.enter(),'ouma');s.award='65';s.zoneLeft=0;s.oumaPending=true;s=a.prepareBet(s,{},()=>.99);assert.equal(s.remaining,'340');assert.equal(a.prepareBet(s,{},()=>0).remaining,'340');});
test('legacy active states retain existing awards without retrospective top-up',()=>{const s=a.settleZone({zone:'urapi',remaining:'50',award:'10'});assert.equal(s.remaining,'330');assert.equal(a.settleZone(JSON.parse(JSON.stringify(s))).remaining,'330');});
