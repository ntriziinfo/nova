import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';
const h=fs.readFileSync('jag.html','utf8');const fn=h.slice(h.indexOf('  function setRandomReel(index){'),h.indexOf('  function setMorningBarReels(){'));
test('reel direction follows normal versus reverse and cancels old cell motion',()=>{for(const reverse of [false,true]){let cancelled=0;const motions=[];const cells=Array.from({length:3},()=>({getAnimations:()=>[{id:'nova-reel-step',cancel(){cancelled++;}}],animate(frames,timing){motions.push({frames,timing});return {};}}));const ctx=vm.createContext({REEL_STRIPS:[['A','B','C','D']],reelSpinOffsets:[0],currentSpin:{artReverse:reverse},REEL_FULL_ROTATION_MS:1800,reels:[{classList:{contains:()=>true}}],mod:(n,m)=>(n+m)%m,ensureReelCells:()=>cells,updateReelCell:()=>{}});vm.runInContext(fn+'setRandomReel(0)',ctx);assert.equal(cancelled,3);assert.equal(motions[0].frames[0].transform,reverse?'translateY(18%)':'translateY(-18%)');assert.equal(motions[0].frames[1].transform,'translateY(0)');assert.equal(ctx.reelSpinOffsets[0],reverse?1:3);}});
