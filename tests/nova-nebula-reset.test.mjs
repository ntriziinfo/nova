import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';import vm from 'node:vm';for(const f of ['nova-art.js','nova-balance.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'));const a=NovaArt;
test('Nebula adds exactly 10pt on every reset and settles once across all three zones',()=>{for(const id of ['toto','sora','ura_sora']){let s={...a.startZone(a.enter(),id),zoneLeft:1,award:'1995'};let t=a.step(s,{},()=>.5,'NEBULA');assert.equal(t.flow.award,'2005');assert.equal(t.flow.zoneLeft,5);assert.equal(t.flow.remaining,'150');assert.match(t.message,/10pt/);s=JSON.parse(JSON.stringify(t.flow));s=a.step(s,{},()=>.5,'NEBULA').flow;assert.equal(s.award,'2015');const end=a.settleZone(s);assert.equal(end.remaining,'2165');assert.equal(a.settleZone(end).remaining,'2165');assert.equal(a.payout('NEBULA'),0);}});
test('exact mean handles ten-point reset increments',()=>{let seed=783;const rng=()=>{seed=(Math.imul(seed,1664525)+1013904223)>>>0;return seed/2**32};for(const id of ['toto','sora','ura_sora']){let sum=0;for(let i=0;i<5000;i++){let s=a.startZone(a.enter(),id);while(s.zone)s=a.step(s,{},rng).flow;sum+=Number(s.remaining)-150;}const mean=NovaBalance.zoneMean(id);assert.ok(Math.abs(sum/5000/mean-1)<.06,id);}});
