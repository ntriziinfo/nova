The p1 candidate data are invalid and must not be used for fitting or validation: reducing BIG to four bell payouts while retaining the original aim hazard produced a negative upper-BIG replay probability. The model now validates every bonus role probability and the analytical AT chance before simulation. The baseline rows use untouched v119 production rules and remain valid as a waveform comparison. Keep these failed raw datasets for audit.
