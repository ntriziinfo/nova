import fs from 'node:fs';import vm from 'node:vm';
for(const f of ['nova-art.js','nova-balance.js'])vm.runInThisContext(fs.readFileSync(f,'utf8'));
let text=fs.readFileSync('SOFT-SETTINGS.md','utf8').replace('通常小役と初当たりBIG/REG振分は全設定共通（従来設定3の値）。通常ベルは約1/6.14、リプレイ約1/7.45。','通常小役は設定順の緩やかな差に変更。確率表とばらつきは ROLE-SETTINGS.md を参照。初当たりBIG/REG振分は全設定共通。');
const start=text.indexOf('|1|'),end=text.indexOf('\n\nCZ分母',start);let rows='';for(let setting=1;setting<=6;setting++){const p=NovaBalance.profile(setting),factor=1+.2*NovaArt.biasFor(setting);rows+='|'+[setting,p.czDenom.toFixed(1),p.directDenom.toFixed(1),(12*factor).toFixed(2)+'%',(40*factor).toFixed(2)+'%',(1/NovaArt.bonusSpecialFor(setting)).toFixed(2)].join('|')+'|\n';}text=text.slice(0,start)+rows.trimEnd()+text.slice(end);fs.writeFileSync('SOFT-SETTINGS.md',text);fs.writeFileSync('../../outputs/設定差とフリーズの変更.md',text);
const data=JSON.parse(fs.readFileSync('../../outputs/normal-verification.json','utf8'));let report='# 裏ゾーン追加後の検証\n\n通常モード・穢れ・フリーズ・6ゾーンを含むゲーム共通ルールで試算。各設定200万有料G×10試行、計1億2,000万G。初期状態は通常A・穢れ0。初期乱数は2222221＋試行番号×98761＋設定。調整後の試算値であり厳密な理論機械割ではありません。\n\n|設定|目標|試算|95%信頼区間|\n|---|---|---|---|\n';for(const r of data)report+='|'+[r.setting,(NovaBalance.targets[r.setting-1]*100).toFixed(1)+'%',(r.mean*100).toFixed(2)+'%',r.ci95.map(x=>(x*100).toFixed(2)).join('〜')+'%'].join('|')+'|\n';report+='\n小役の設定差・ART直撃の最新値は ROLE-SETTINGS.md を参照。以前の設定で作成した短期RTP分布は今回の検証結果に含めません。特化ゾーンや当選までの待機を含む全体の純増は、通常ART・ボーナス中の期待純増2.5pt/Gとは別です。\n';fs.writeFileSync('NORMAL-VERIFICATION.md',report);fs.writeFileSync('../../outputs/設定差調整後の検証.md',report);
