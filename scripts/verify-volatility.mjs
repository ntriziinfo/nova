import fs from 'node:fs';import {simulate} from './simulate-normal.mjs';
const rows=[];const trials=Number(process.argv[3]||300);for(const games of [3000,10000])for(let setting=1;setting<=6;setting++){const runs=Array.from({length:trials},(_,i)=>simulate(setting,NovaBalance.profile(setting).scale,games,446677+setting+i*98567));const nets=runs.map(r=>r.net).sort((a,b)=>a-b),q=p=>nets[Math.floor(p*(nets.length-1))];const row={games,setting,trials,p10:q(.1),median:q(.5),p90:q(.9),loss:runs.filter(r=>r.net<0).length/trials,win3000:runs.filter(r=>r.net>=3000).length/trials,drawdown3000:runs.filter(r=>r.maxDrawdown>=3000).length/trials,art1000:runs.filter(r=>r.maxArtNet>=1000).length/trials,deep500:runs.filter(r=>r.maxNormalGames>=500).length/trials};rows.push(row);console.log(JSON.stringify(row));}fs.writeFileSync('../../outputs/volatility-'+(process.argv[2]||'after')+'.json',JSON.stringify(rows,null,2));
