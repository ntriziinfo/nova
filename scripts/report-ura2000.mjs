import fs from 'node:fs';import crypto from 'node:crypto';import './simulate-normal.mjs';
const a=NovaArt,rows=[];for(let setting=1;setting<=6;setting++){const w=a.soraZoneWeights[setting-1].map(x=>x/100),u=a.soraUraZoneWeights[setting-1].map(x=>x/100),normal=[160,215,300,NovaBalance.giruMean(setting),0,650],ura=[600,600,905,NovaBalance.giruMean(setting,true),0,2000];const other=w.reduce((v,p,i)=>i===4?v:v+p*(.97*normal[i]+.03*ura[i]),0),m=3.69,A=893.75+.75*other,B=275+m*.75*275+m*.25*(u[0]*normal[3]+u[2]*normal[5]+u[3]*ura[3]+u[5]*ura[5]),bn=.75*w[4]*.97,cn=.75*w[4]*.03,bu=m*.25*u[1],cu=m*.25*u[4],d=(1-bn)*(1-cu)-cn*bu;const N=(A*(1-cu)+cn*B)/d,U=(B*(1-bn)+bu*A)/d;if(U<1900||U>2100)throw Error('Ura Sora out of target range');rows.push({setting,normalSora:N,uraSora:U,samples:JSON.parse(fs.readFileSync(`ura2000-zone-${setting}.json`))});}
const hashes=Object.fromEntries(['nova-art.js','nova-balance.js','scripts/measure-ura2000-zones.mjs'].map(f=>[f,crypto.createHash('sha256').update(fs.readFileSync(f)).digest('hex')]));fs.writeFileSync('URA-2000-V30.json',JSON.stringify({rows,hashes},null,2));
const md=['# 裏上乗せゾーン 平均約2,000pt V30','','初期保証込み、1セット275pt換算、獲得ゾーンの連鎖込みの平均上乗せ。特化消化中の払い出し/BETやAT消化中の再当選は別。','','- 裏ギル: 低額域継続率87.320257%→89.024018%。段階減衰65%・20%は維持。平均1,792.3pt→2,000pt。','- 裏逢魔: 裏上乗せ係数3→38/11。平均1,750pt→2,000pt。','- 空: 7揃い75%で1セット、25%でゾーン予約。通常空のゾーンは前案6種。裏空は通常ギル/空/逢魔と裏ギル/空/逢魔の指定6種。裏合計20〜30%の最終配分。','- 裏空: nebulaリセット込みの平均7揃い回数6→3.69（全設定共通）。初期1セット保証と10Gリセット維持。','- 初期保証はギル・逢魔100pt、空1セット。獲得ゾーンは空終了後に放出。通常報酬への追加裏昇格なし。','','|設定|通常空 平均上乗せpt相当|裏空 平均上乗せpt相当|','|---|---:|---:|',...rows.map(r=>`|${r.setting}|${r.normalSora.toFixed(1)}|${r.uraSora.toFixed(1)}|`),'','各設定各裏ゾーン10,000回、計180,000回を検証。裏ギル/裏逢魔は設定共通。全設定合算のサンプル平均: '+[0,1,2].map(i=>['裏ギル','裏空','裏逢魔'][i]+' '+(rows.reduce((v,r)=>v+r.samples[i].mean,0)/6).toFixed(1)+'pt').join('、')+'。','', '台全体の機械割・勝率は今回の変更後は未再測定。以前の試算を新仕様の値として使用しない。'];fs.writeFileSync('URA-2000-V30.md',md.join('\n')+'\n');console.log(md.join('\n'));
