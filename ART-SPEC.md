# ART移行・上乗せゾーン仕様（2026-09-07）

ユーザー指定の新仕様を試作実装。以下の未確定事項には末尾の仮設定を適用した。仮設定をユーザーの確定指定と扱わない。

## ゲームフロー
- 通常 → CZ／強CZ → BIG・REGまたはART → ART。
- BIG・REG終了後はARTへ。
- 高設定ほどART直撃率が高い。通常からの直撃とCZ経由のART振り分けの具体的な抽選箇所・数値は未確定。
- ART中はレア役で上乗せゾーンとBIGを抽選。
- ART初期G数・純増・終了条件・ボーナス中のART残りG数の扱いは未確定。以前のRT50G・1.2ptを自動的に引き継がない。

## 6種類の上乗せゾーン
|ゾーン|継続|内容|平均上乗せ目標|
|---|---|---|---|
|宗介ゾーン|3G|成立役で上乗せG数を抽選|20G|
|ととゾーン|5G|最終告知。上乗せ期待度に応じてランプ色を変化|30G|
|うらぴゾーン|未指定|特殊図柄を揃えると上乗せ。特殊図柄は後日指定|40G|
|ギルゾーン|1G＋0G連|初期5G。50%で逆回転。逆回転停止後、BETごとに50%でさらに逆回転。成功ごとに上乗せG数を倍化|未指定|
|空ゾーン|10G|7を狙え高確率。7揃いのたびにボーナスをストック|G数上乗せではなくボーナスストック|
|逢魔ゾーン|未指定|ノヴァ揃い高確率。弱・強・スーパーの種類で上乗せ。本機最強の特化ゾーン|未指定|

### ととゾーン
ユーザー記載の色順は「白＞青＞黄＞緑＞赤＞レインボー」。期待度の高低方向と各色の閾値・告知方法（累計G数など）は実装前に確定する。

### ギルゾーン
倍化系列は5→10→20→40→80…G。倍化後の最終値を加算するか、成功ごとの値を累積加算するかは未確定。
ユーザー追加指定により、上乗せG数の上限なしで確定。50%継続に失敗するまで倍化を続ける。実装で独自の上限や回数制限を加えない。
0G連中のART残りG数・BETのpt消費は未確定。
50%継続×2倍を上限なしで繰り返すため、理論平均は有限値にならない。この性質は説明済みで、ユーザーは上限なしを選択した。再確認を要求しない。実装時は大きなG数の数値精度・保存・表示を考慮する。

### 空ゾーン
ストックのボーナス種別、7揃い確率、告知・放出順序は未確定。

### 逢魔ゾーン
通常時のノヴァ揃いとは別処理。通常スーパーのBIG／フリーズ50:50を自動適用しない。
継続G数・各ノヴァ揃い確率・各上乗せG数・保証・終了条件は未確定。

## 共通の未確定事項
- レア役の定義・確率。廃止済みのチェリー・ピエロを勝手に復活させない。
- ART中のBIG・各ゾーンの当選率、設定差、重複当選、待機・放出順序。
- 平均20／30／40Gを満たす役別抽選テーブル。
- 既存フリーズ/PBBの新フローへの接続。
- 提供イラスト・音源は学習・画像生成へ使用しない。原本を保持し、許可された表示上の色・明暗変更のみ行う。

## 試作実装の仮設定
- ART初期50G。通常消化はリプレイ60%・ベル30%・ハズレ10%を基礎に、3%でレア役（仮に弱ノヴァ目）へ置換。全体純増・機械割は再計算前。
- ART直撃分母：設定1から1800／1500／1200／900／600／400。通常で直撃を先に抽選し、非直撃時に従来のCZ抽選。
- CZ成功時25%でART、残り75%で従来比率のBB／RB。CZ10G・40%、強CZ10G・70%は維持。
- ARTレア役でBIG12%、ゾーン40%、その他48%。ゾーンは6種類均等。重複当選なし。
- ART中のボーナス・ゾーンでは残りART G数を保持。ARTが0Gのボーナス終了後は初期G数を付与。通常・CZからのボーナス後も初期G数。PBBは既存2回連続後にARTへ。
- 宗介：ベル10G、リプレイ5G（1/18で10G）、ハズレ5G。標準役配分で3G平均20G。累計を終了時加算。
- とと：ベル10G、リプレイ5G、ハズレ0G。5G平均30G。途中はG数非告知。累計0〜9白／10〜19青／20〜29黄／30〜39緑／40〜49赤／50以上虹で、右側ほど高期待。終了時に累計を告知・加算。
- うらぴ：5G、図柄揃い40%、1回20G（平均40G）。正式図柄の指定までは強ノヴァ目を仮図柄として表示。
- ギル：最終到達G数を1回加算。最初の1Gは3pt、以後の0G連はpt・回転数・ART G数を消費しない。逆回転はリール配列の進行方向を反転。G数は文字列＋BigInt計算で上限なし・正確に保存。
- 空：10G、7揃い25%、1回ごとにBIGを1個ストック。ゾーン終了後に順次放出してARTへ復帰。
- 逢魔：5G、ノヴァ揃い60%。揃い時の内訳は弱70%／強25%／スーパー5%、上乗せ20／50／200G。通常時のBIG・フリーズ抽選とは分離。
- ART初期G数・直撃分母・レア役率・BIG/ゾーン率・CZのART振分・うらぴ/逢魔G数と揃い率・空の7率はデバッグで変更・保存可能。
- デバッグ強制：ART、ARTレア役、6ゾーンを追加。既存ノヴァ目強制も維持。
- 既存RTセーブは残りG数を保ってARTへ移行。確定筐体配置・提供画像・音源は変更しない。
- 検証：21テスト（各ゾーン、CZ/ART終了、ストック、逢魔分離、ギル1100連倍化・保存、既存図柄・配当）。ブラウザでゾーン突入・残り保存復帰・空7ストック・ととランプ・高速消化を確認。
